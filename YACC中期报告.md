# YACC 中期报告

- 项目仓库链接：https://github.com/twilight0702/c99-yacc-lr-lalr-practice
- 学号：71123226
- 姓名：于悦

## 1. 截至目前已完成工作

### 1.1 主线功能完成情况

当前已完成从文法输入到 LR/LALR 解析执行主链路的实现与集成：

1. 输入范围定义与第一版边界固化。
2. 文法数据结构设计（符号、产生式、索引、动作占位）。
3. `.y` 文件解析器（支持 `%token`、`%start`、规则段、动作块保留、不支持项显式报错）。
4. 文法预处理与增广文法构造。
5. First 集与可空性计算。
6. LR(1) 项、I0 闭包与 Goto 构造。
7. LR(1) 规范族与状态转移图构造。
8. Action/Goto 表构造、冲突检测与冲突消解日志输出。
9. 运行时移进-归约解析器（LR1/LALR 双轨）、解析轨迹与错误信息导出。
10. LR(1) 到 LALR(1) 状态合并、表重建与差异对比。

### 1.2 工程化与工具链建设

1. 已形成统一命令行入口：`build/src/yacc_parse_tool`，可串联核心流程。
2. 已形成结构化导出机制：在 `artifacts/yacc/` 下按阶段与 case 输出 `summary.txt + raw/ + analysis/`。
3. 已形成可视化链路：`scripts/yacc_visualizer_prepare.py` 将产物转换到 `visualizer/public/data/v1/*`。
4. 已形成测试体系：
   - 测试1：golden 回归
   - 测试2：与 bison 行为对拍
   - 测试2f：完整 LALR 状态机对拍
   - 测试3：表结构一致性校验
   - 测试4：解析 trace 不变量校验

### 1.3 当前量化结果（c99 基线）

1. 文法规模：符号 158（终结符 87，非终结符 68），产生式 238（含增广）。
2. LR(1) 规范族：状态 1855，转移边 17745。
3. LR 分析表：Action 表项 30080，Goto 表项 7489，冲突 2。
4. LALR 合并效果：LALR 状态 399，相比 LR1 合并 1456 个状态。
5. 冲突对比：LR1 冲突 2，LALR 冲突 1。
6. 测试样例覆盖：当前 token 用例 11 组，golden 用例目录 11 组（覆盖合法与非法输入）。

### 1.4 可视化进展

1. 已完成可视化前端侧栏与总览页，支持按 case 查看各阶段数据。
2. 已完成关键页面：输入解析、文法预处理、LR(1) 规范族、LR 分析表、解析轨迹、LALR 合并对比。
3. 已接入图形化组件展示状态图与统计卡片，可直观看到状态数、转移边数、冲突数和解析结果。
4. 已具备“原始产物 -> JSON 协议 -> 页面渲染”的固定数据通道，当前已提供 `c99` 用例的数据展示。

## 2. 当前未完成项与阶段风险

1. 与 `c99.l` 的深度联调尚未完成（token 编号、命名、属性值协议统一）。
2. 语义动作执行框架与 AST/中间表示接口尚未接入。
3. 错误恢复仍以“报错定位+期望符集合”输出为主，未形成更强恢复策略（如同步符号恢复、短语级恢复）。

## 3. 下一步工作计划

### 3.1 第一优先级（词法-语法联调）

1. 统一 `c99.l` 与 `c99.y` 的 token 枚举与命名映射，固化 token 协议文档。
2. 打通“源代码 -> lexer token 流 -> parser”自动链路，替代当前手工 token 文件喂入为主的验证方式。
3. 在现有 11 组样例基础上增加端到端输入样例，并纳入 `tests/run_yacc_tests.py`。

### 3.2 第二优先级（语义接口预留）

1. 以规约序列为触发点，设计 `production_id -> semantic action` 绑定接口。
2. 定义语义值栈与节点构建接口，先完成 AST 骨架（声明、函数定义、表达式基础节点）。
3. 衔接中间表示阶段，确保语法层输出可直接被后续模块消费。

### 3.3 第三优先级（质量收敛与展示）

1. 扩展冲突分析与错误恢复测试，补充失败样例分层统计。
2. 持续更新可视化数据，沉淀答辩用“状态数、冲突数、样例覆盖率、通过率”指标。
3. 增加可视化中的对比视图（LR1 与 LALR 关键指标同屏对比、典型错误样例回放）。
4. 完成阶段性回归脚本的一键执行（构建、导出、测试、可视化更新）。
